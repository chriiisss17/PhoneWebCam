import React from 'react';
import { ActivityIndicator } from 'react-native';

const Loading = () =>{
return(
    <ActivityIndicator
    color={'red'}
    size={'large'}
    />
)

}

export default Loading;